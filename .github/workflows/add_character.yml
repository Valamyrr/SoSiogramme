name: Ajouter un personnage au CSV
on:
  issues:
    types: [opened,labeled]

permissions:
  contents: write
  pull-requests: write

jobs:
  add-character:
    if: |
      contains(github.event.issue.labels.*.name, 'ok') &&
      contains(github.event.issue.labels.*.name, 'personnage')
    
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4

      - name: Ajouter personnage au CSV
        run: python add_character.py
        env:
          ISSUE_BODY: ${{ github.event.issue.body }}

      - name: Créer PR
        uses: peter-evans/create-pull-request@v5
        with:
          title: "Ajout personnage #${{ github.event.issue.number }}"
          branch: ajout-personnage-${{ github.event.issue.number }}
